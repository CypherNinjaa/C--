#include <iostream>
using namespace std;
int main()
{
    // cout << 2 % 40;
    // if a>b -> a%b -> reminder
    // if a==b -> a%b -> 0
    // if a<b -> a%b -> a
    cout << -99 % -4;
    // if a % b = a%(-b) = a % b
    // if -a % b = -reminder
    // if (-a) % (-b) = -a % b 
    // due to hierarchy of operators like BODMAS
}